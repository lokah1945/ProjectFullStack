// cms-strapi/config/plugins.ts
// In Strapi v5, i18n is built into the core and no longer a separate plugin.
// Locales are managed via the admin panel (Settings → Internationalization).
// The default locale 'en' is created automatically.
// Additional locales (e.g. 'id') are created in the seed bootstrap (src/index.ts).
export default () => ({});
